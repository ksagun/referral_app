export interface ReferralState {
  createReferral: {
    response: any;
    error: any;
    isLoading: boolean;
  };
  referrals: {
    response: any;
    error: any;
    isLoading: boolean;
  };
  referrerAcount: {
    response: any;
    error: any;
    isLoading: boolean;
  };
}
